language: node_js
node_js:
    - 12
cache: false
install:
    - npm install
jobs:
    include:
        - stage: validate
          script: npm run validate
        - stage: test
          script: npm run test
